# Backend Environment Variables
# Copy this to backend/.env file

# MongoDB Configuration
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/faceAuthDB

# Server Configuration
PORT=5002
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# For production:
# NODE_ENV=production
# MONGO_URI=your_production_mongo_uri
# FRONTEND_URL=https://visual-math-frontend.onrender.com

# Optional API Keys (if needed for future features)
# TWILIO_AUTH_TOKEN=your_token
# JWT_SECRET=your_secret_key
